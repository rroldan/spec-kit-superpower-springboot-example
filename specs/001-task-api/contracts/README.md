# Contracts for Task API

This folder contains the OpenAPI contract for the Task API endpoints for this feature.

Files:
- `openapi.yaml` — OpenAPI 3.0.3 snippet describing `POST /api/v1/tasks` and schemas (Task, ErrorResponse, CreateTaskRequest).

Usage:
- Use this file as the source of truth for client generation, contract tests, and CI validations.
